query GetAppleMusicArtistTopSongs($artistId: String!) {
  getAppleMusicArtistTopSongs(artistId: $artistId) {
    id
    name
    releaseDate
    
    artists {
      name
      id
    }
      
    lengthMs
    previewUrl
  }
}
