//
//  ProjectCoverDetailView.swift
//  STARS
//
//  Created by Marius Gabriel Budăi on 22.11.2024.
//

import SwiftUI
import SDWebImageSwiftUI

struct ProjectCoverDetailView: View {
    /*@Environment(\.dismiss) private var dismiss
     
    var cover: ProjectCover*/
    
    var body: some View {
        /*NavigationView {
            VStack {
                WebImage(url: URL(string:cover.image))
                    .resizable()
                    .frame(width: 256, height: 256)
                    .clipShape(RoundedRectangle(cornerRadius: 10))
                    .shadow(radius: 10)
                
                Spacer()
            }
            .navigationTitle("Reviews")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .confirmationAction) {
                    Button("Done") {
                        dismiss()
                    }
                }
            }
        }*/
    }
}

#Preview {
    /*@Previewable  
    ProjectCoverDetailView(cover: dataManager.projectCovers.first!)
         */
}
