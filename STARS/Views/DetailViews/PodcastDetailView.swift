//
//  PodcastDetailView.swift
//  STARS
//
//  Created by Marius Gabriel Budăi on 02.10.2024.
//

import SwiftUI
import SDWebImageSwiftUI

struct PodcastDetailView: View {
    /* 
    
    var podcast: Podcast*/
    var body: some View {
        /*ScrollView {
            VStack {
                ZStack {
                    Rectangle()
                        .foregroundColor(.accentColor)
                    //.padding(.top, -50)
                        .frame(height: 120)
                        .shadow(radius: 10)
                    
                    WebImage(url: URL(string:podcast.covers.first!))
                        .resizable()
                        .frame(width: 256, height: 256)
                        .clipShape(RoundedRectangle(cornerRadius: 10))
                        .shadow(radius: 10)
                    //.padding(.top, -50)
                    
                }
                .padding(.top, -50)
                
                Text(podcast.title)
                    .font(.title2)
                    .bold()
                    .multilineTextAlignment(.center)
            }
        }
        .navigationBarTitleDisplayMode(.inline)*/
    }
}

#Preview {
    /*@Previewable  
    PodcastDetailView(podcast: dataManager.podcasts.first!)
         */
}
