//
//  FeatureCard.swift
//  STARS
//
//  Created by Marius Gabriel Budăi on 27.08.2024.
//

import SwiftUI

struct FeatureCard: View {
    /*var project: Project*/
    
    var body: some View {
        Text("Feature card")
        /*HStack {
            VStack() {
                Text(project.title)
                    .font(.title2)
                    .bold()
        
                Text(project.artist)
                    .padding(.top, 5)
                    .padding(.bottom, 10)
                
                HStack {
                    Text("5.0")
                    ForEach(1..<6, id: \.self) { value in
                        Image(systemName: "star")
                    }
                    .foregroundColor(.yellow)
                }
                .font(.title3)
                .padding()
            }
            Image(project.cover)
                .resizable()
                .frame(width: 175, height: 175)
                .padding()
        }
        .background(FeatureCardBackground())
        .frame(height: 200)
        .padding()*/
    }
}

#Preview {
    /*FeatureCard(project: DataManager().projects[0])*/
}
