//
//  SongArtistNameView.swift
//  STARS
//
//  Created by Marius Gabriel Budăi on 28.08.2024.
//

import SwiftUI

struct SongArtistNameView: View {
    /* 
    let song: Song
    var linkToArtists = false
    @State private var artists: [Artist] = []*/
    
    var body: some View {
        /*HStack {
            ForEach(Array(artists.enumerated()), id: \.element.id) { index, artist in
                if linkToArtists {
                    NavigationLink {
                        ArtistDetailView(artist: artist)
                    } label: {
                        Text(artist.name)
                    }
                }
                
                else {
                    Text(artist.name)
                }
                
                if index < artists.count - 1 {
                    Text("&")
                }
            }
        }
        .onAppear {
            DispatchQueue.main.async {
                dataManager.fetchArtists(withIDs: song.artist) { fetchedArtists in
                    artists = fetchedArtists
                }
            }
        }*/
    }
}

#Preview {
    /*@Previewable  
    SongArtistNameView(song: dataManager.songs.first!)
         */
}
